# Homebrew tap — tpatrouillat/tap

Public formula repo. Product *why* is Tokease Brain, not this tree.

- Short brief (loaded every turn): `~/ThibOS/brain/projects/Tokease/context.md`
- Long: `journal.md` and `decisions/` in that Brain folder — open on demand, never `@`import.
- Same session as a formula bump: if the release changes a **product** promise (what we claim, freeze, version users see), update that `context.md`. SHA/caveats-only stays here.
- No daily hygiene session. Graphify: none (formulae only).
